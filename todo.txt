## OldSchoolGames V2 Backend - TODO
## Branche courante : 18-gestion-reversi

=== AMÉLIORATION CODE EXISTANT ===

[x] GridGame.ts
    - Morpion.ts redéfinit localement `vectors` → supprimé, utilise this.vectors.
    - Puissance4.ts idem → corrigé.
    - checkPlay : ordre des vérifications corrigé (locked en premier).
    - Getters getPlayer1() / getPlayer2() ajoutés.
    - play() déclaré abstract.

[x] gamesEvents.service.ts
    - Map typée GridGame, game.player1/player2 remplacés par les getters.
    - Message d'erreur corrigé (game object → roomName string).

[x] Puissance4.ts - Bugs corrigés
    - winningCells.push() avant aligned++ (indices décalés).
    - Côté opposé : alignedOpposite utilisé correctement.
    - Boucle recherche cellName : partait de i=6 (c${col}0 inexistant) → i>=1.
    - Colonne pleine : erreur explicite ajoutée.

[x] Puissance4.ts - Wrap-around corrigé
    - Guard colOf() appliquée dans les deux boucles while d'extension (forward et backward).
    - Même logique que la guard déjà présente dans la sélection des validvectors.

[ ] Puissance4.ts / Morpion.ts - Complexité cognitive trop élevée
    - Puissance4.play() score 43 (max recommandé : 15).
    - Extraire la logique de victoire dans une méthode privée checkVictory().
    - Morpion.play() à vérifier aussi.

[x] gamesEvents.service.ts - Corrections v2
    - GAMES_REGISTRY marqué private readonly.
    - Guard !data.roomName ajouté.
    - Guards !game sur play et reload.
    - handleGameEvent découpé en méthodes privées (complexité cognitive réduite).
    - filter()[0] remplacé par find().
    - logger et games marqués readonly.
    - Mutation result.turn remplacée par spread operator.
    - handleGameCreation : vérification room.length < 2 ajoutée.

[x] userEvents.service.ts
    - Double deleteMany supprimé (OR fromId/toId suffit).
    - JwtService injecté via constructeur (plus de new JwtService()).
    - logger marqué readonly.
    - JwtModule ajouté dans events.module.ts.

[x] invitationEvents.service.ts
    - logger inutilisé supprimé.
    - Vérifications !response inutiles supprimées (Prisma throw sur échec).
    - crash senderSocket null corrigé dans acceptInvitation (guard + return).
    - Émission sur mauvaise room supprimée (ligne orpheline server.to(invitationId)).
    - checkCanceledInvitation implémenté (invitationId requis, invitation existante, appartient au client).
    - Fautes de frappe corrigées (Iinvtation → Invitation).
    - result 'accepted' dédupliqué via variable partagée.

[ ] gamesEvents.service.ts
    - Enregistrer ReversiGame dans GAMES_REGISTRY une fois Reversi implémenté.

[x] auth.service.ts - Corrections
    - avatarMessage orphelin corrigé : assigné dans le catch du renommage avatar.
    - console.error remplacé par this.logger.error (Logger NestJS).

[x] auth.service.ts / auth.controller.ts - Corrections restantes
    - login() : LoginDTO créé, ValidationPipe branché.
    - SALT_ROUNDS extrait dans src/commons/utils/env.ts (auth + users).
    - eventsGateway.notifyUserRegistered() implémenté et appelé à l'inscription.
    - node:fs, Number.parseInt, condition négative login() corrigés.
    - @types/bcrypt installé.

[ ] Vulnérabilités npm - résidu non corrigeable sans breaking change
    - multer <=2.0.2 (high, prod) : fix downgrade @nestjs/core → bloquant.
    - ajv (moderate, devDep) : fix downgrade @nestjs/cli@7 → bloquant.
    - serialize-javascript + webpack (high, devDep) : fix downgrade ts-loader@9.2.1 → bloquant.
    - À traiter lors d'une mise à jour majeure NestJS (hors feature branch).
    - ConfigModule NestJS (@nestjs/config) : à envisager pour centraliser les variables d'env
      (remplacerait la constante SALT_ROUNDS dans commons/utils/env.ts).

[x] auth.controller.ts + users.controller.ts - Multer config dupliquée
    - avatarMulterConfig extrait dans src/commons/multer.config.ts.
    - console.log → logger.error dans fileUpload.ts.
    - require('sanitize-filename') → import ESM.

=== INFRASTRUCTURE ===

[ ] Gestion des logs en fichier
    - Remplacer/compléter le Logger NestJS par un logger persistant (ex: winston, pino).
    - Permettre le monitoring de l'application en production.
    - À faire hors feature branch active.

[ ] Migration Prisma 7
    - Prisma 6 actuellement installé (^6.14.0).
    - Prisma 7 déplace la config de connexion de schema.prisma vers prisma.config.ts.
    - À faire hors feature branch active.
    - Refs : https://pris.ly/d/config-datasource

=== COHÉRENCE BACKEND / FRONTEND ===

[x] IauthResponse.ts - avatarMessage manquant
    - Backend envoie avatarMessage? lors du register (non-bloquant).
    - Interface frontend mise à jour : avatarMessage?: string ajouté.

[x] Dashboard.tsx - eventType 'registered' géré
    - Notification bandeau : "xxx vient de rejoindre OldSchoolGames !"
    - 'connected' : "xxx vient de se connecter"
    - 'disconnected' : "xxx vient de se déconnecter"
    - Composant NotificationFeed créé (auto-expiration 5s, slide-in animation).

[x] IUsers.ts - eventType 'registered' ajouté
    - IUserEventData : 'connected' | 'disconnected' | 'registered'.

[x] LoginForm.tsx - avatarMessage affiché
    - Bandeau warning orange si avatarMessage présent dans la réponse register.
    - Bouton "Continuer" pour naviguer vers / manuellement.
    - errorRef DOM remplacé par state React (errorMessage).

[x] event 'welcome' supprimé
    - Bruit inutile côté client : la distinction connected/registered est gérée via les events broadcast aux autres.
    - client.emit('welcome', ...) supprimé de userEvents.service.ts.

[x] event 'error' (game) géré
    - Backend envoie error dans result{} (pas dans eventType) — IGameEventData corrigé en conséquence.
    - Morpion et Puissance4 : guard result?.error ajouté en tête du handler, affiche l'erreur via setTurn.

=== REVERSI ===

[x] Reversi.ts - Logique complète implémentée
    - Init plateau : c44/c55 = player1 (white), c45/c54 = player2 (black)
    - getCellsToFlip() : collecte les pions adverses à retourner sur tous les vecteurs valides
    - Guard colOf() + expectedColDelta() : anti wrap-around (même logique que Puissance4)
    - hasValidMove() : vérifie si un joueur peut jouer
    - computeResult() : décompte des pions → winner ou draw
    - Cas pass : adversaire sans coup → joueur rejoue, pass inclus dans la réponse
    - Fin de partie : aucun coup possible pour les deux joueurs
    - ReversiGame enregistré dans GAMES_REGISTRY
    - IGameEventData frontend : flippedCells? et pass? ajoutés

[x] Reversi.tsx - Page frontend implémentée
    - Grille 8x8 via GameBoard, animation flip SVG (ellipse) pour les retournements
    - Cas pass affiché via setTurn
    - Reconnexion : getGameData renvoie cells, plateau restauré au montage
    - Route /reversi ajoutée dans main.tsx
    - getCells() ajouté à GridGame, renvoyé dans handleGetGameData
    - IGameEventData : cells? typé

=== README.md ===

[ ] Mettre à jour la structure de fichiers (ajouter Puissance4/, Reversi/)
[ ] Ajouter Puissance4 dans les fonctionnalités principales
[ ] Mettre à jour la branche courante et la date
[ ] Documenter les design patterns utilisés (héritage GridGame, Registry pattern)

[x] users.service.ts / users.controller.ts
    - UpdateMeDTO créé (src/users/DTO/updateMe.dto.ts).
    - updateMe typé avec UpdateMeDTO, plus de any ni d'injection de champs arbitraires vers Prisma.
    - prismaData construit explicitement (pseudo, avatarUrl, password uniquement).
    - fs.unlinkSync protégé par fs.existsSync.
    - ValidationPipe + DTO branché dans le controller.
    - req typé Request & { user: { id: number } }.

=== TODO DU README (backlog existant) ===

[ ] Exporter la validation d'avatar vers un service dédié
[ ] Notifier les WebSocket lors d'une nouvelle inscription
[ ] Gestion complète des erreurs en cas d'utilisateur manquant
[ ] Validation complète lors de la création du jeu
[ ] Vérification de validité pour l'annulation d'invitation
[ ] Notifications WebSocket pour annulation d'invitation
[ ] Gestion des salles de jeu des utilisateurs déconnectés
[ ] Refactorisation centralisée de gestion d'erreurs
